const { ITEM_META, LEVEL2_SINGULAR, FORM_KIND_META } = require('./metadata');
const { TRANSLATION_MAP } = require('./translations');

module.exports = {
  ITEM_META,
  LEVEL2_SINGULAR,
  FORM_KIND_META,
  TRANSLATION_MAP
};
